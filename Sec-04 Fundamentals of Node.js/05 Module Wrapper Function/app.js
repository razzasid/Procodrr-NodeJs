const a = 5; // The variable 'a' will be created in the local scope with and without 'strict mode'.
b = 10 // The variable 'b' will be created in the global scope without 'strict mode', and wwith 'strict mode' it will give an error. 
console.log(5)
console.log(b)